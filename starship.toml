add_newline = false

[git_branch]
symbol = "🌱 "
truncation_length = 18
truncation_symbol = "…"

[git_status]
ahead = "⇡${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
behind = "⇣${count}"

[python]
pyenv_version_name = true

[nodejs]
disabled = true
